# A metaclass is a class of a class, which means that the class is an instance of its metaclass.
# With metaclasses, programmers can get an opportunity to create classes of their own type from the 
# predefined Python classes.

